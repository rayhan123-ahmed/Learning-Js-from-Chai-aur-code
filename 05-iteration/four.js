const myObject ={
    js: "javascript",
    swift : "swift by apple",
    rb: "rubby",
    cp: "c++"
}

for (const key in myObject) {
//    console.log(`${key} shortcute for ${myObject[key]} `);
}

const programing = ["java","paython","c++","rb"]

for (const key in programing) {
    console.log(programing[key]);
}